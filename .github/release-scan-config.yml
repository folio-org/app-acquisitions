scan_config:
  enabled: true
  
  # Reviewers to add to auto-generated PRs (comma-separated)
  # Use format: username for users, org/team-name for teams
  pr_reviewers: ''

release_branches:
  - R1-2025
  - R2-2024
  - R2-2025